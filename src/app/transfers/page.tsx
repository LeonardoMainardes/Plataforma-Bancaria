// import { useTransfer } from "@/hooks/use-transfer";

export default function TransferPage() {
  // const { balance, transfer, status, errorMessage, reset } = useTransfer()
  return <div>Transfers</div>;
}
