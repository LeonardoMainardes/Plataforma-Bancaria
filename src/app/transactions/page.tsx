// import { useTransactions } from "@/hooks/use-transactions";

export default function TransactionsPage() {
  // const { transactions, loading, error, search, setSearch, filter, setFilter } = useTransactions();
  return <div>Transactions</div>;
}
